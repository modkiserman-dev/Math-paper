$pdf_mode = 1;

# Put all LaTeX-generated output into build/
$out_dir = 'build';
$aux_dir = 'build';

$interaction = 'nonstopmode';
$halt_on_error = 1;

# Copy the final PDF to the repository root after successful compilation.
$success_cmd = 'cmd /c copy /Y build\main.pdf ..\main.pdf';
